<!-- sidebar -->
  <div class="sidebar">
<!--    <div id="map">-->
<!--      <h5> Where in the world are you? </h5>-->
        <%#= image_tag("/assets/world_map.jpg",  alt: "map", class: "sidebar_image") %>
<!--      </div>-->
<!--    <br>-->

    <div id="weather">
      <h5> Weather </h5>
      <% if current_user %>
       <p>Current: <%= weather[:temperature] if weather %> degrees Celsius with <%= weather[:sky] if weather %>.</p>
        <p>The forecast temperature range for today is between
          <%= weather[:min_t] if weather %> and
        <%= weather[:max_t] if weather %> degrees Celsius.</p>
    <% else %>
        Register or login to view your local weather.
        <%= image_tag("/assets/weather.jpg",  alt: "weather", class: "sidebar_image") %>
    <% end %>
    </div>
  </div>

